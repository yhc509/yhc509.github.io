# Project Rules

이 저장소에서 `content/projects/*.md`를 추가하거나 수정할 때는 아래 규칙을 따릅니다.

## Frontmatter

- 필수 필드: `title`, `date`, `description`, `thumbnail`, `tags`, `open`, `role`, `highlights`, `links`
- `tags`는 1뎁스만 사용합니다.
- `highlights`는 정확히 3개를 유지합니다.
- `links`는 공개 가능한 항목만 넣습니다.
- 링크 종류는 `github`, `demo`, `docs`, `devlog`만 사용합니다.
- 공개 프로젝트는 위 링크 중 최소 1개가 있어야 합니다.
- `featured` 같은 대표 표시 필드는 두지 않습니다.

## 본문 구조

- 섹션 순서는 `## 개요`, `## 문제/배경`, `## 핵심 구현`, `## 링크`
- 불필요한 섹션을 늘리지 않습니다.
- `배운 점`, `느낀 점`, `회고`, `구성 요약` 같은 섹션은 쓰지 않습니다.

## 문체

- 사실만 짧게 적습니다.
- 당연한 설명, 메타한 문장, 과장, 홍보 문구는 쓰지 않습니다.
- README처럼 건조하게 씁니다.
- 리스트 카드에 보이는 `highlights`는 한 줄로 바로 이해되는 문장으로 적습니다.
- 목록은 `썸네일 -> 제목 -> highlights 3개 -> 상세 보기` 순서만 사용합니다.
